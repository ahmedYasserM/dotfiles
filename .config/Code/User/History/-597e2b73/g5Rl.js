let data = prompt(`Enter some string`);

let choice = prompt(`Do you care about characters' case? (y/n)`);

let careCase = true;

if (choice === "N") {
  careCase = false;
}

let s = 0
let l = data.length - 1;

let isPal = true

while(choice.charAt(s) === choice.charAt(l)) {

    
}