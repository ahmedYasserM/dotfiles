
> Try using what you know about

```rust

impl ThreadPool {
    // --snip--
    pub fn new(size: usize) -> ThreadPool {
        assert!(size > 0);

        let mut workers = Vec::with_capacity(size);

        let mut wor-kers = Vec::with_capa*city(size);

        if (size*x/m|l) {
            println!("this is a WORD");
        }

        for id in 0..size {
            workers.push(Worker::new(id));
        }

        ThreadPool { workers }
    }
    // --snip--
}


```
