

# dolphin
alias do="setsid dolphin $1 &> /dev/null"

### nnn
alias n="nnn"

# vritualvox
alias vbox="vboxmanage"

# jenkins-cli
alias jcli="java -jar /opt/java-apps/jenkins-cli.jar -s http://localhost:8080/" 


alias yless="jless --yaml"

# vim
alias vim='nvim'


# tmux
alias tl='tmux ls'
alias tns='tmux new-session -t'
alias ta='tmux attach -t'
alias td='tmux detach'


# youtube-dl
alias ydl='youtube-dl'

# apt
# alias r='sudo apt remove' ## remove
# alias q='sudo apt search' ## query
# alias i='sudo apt install' ## install
# alias s='sudo apt update' ## sync
# alias u='sync;sudo apt upgrade' ## update

# ls
alias cl='command ls --color=auto' ## pure ls
alias ls='exa --icons'
alias la='ls -a'
alias ll="ls -l"
alias lla="ls -lah"

# Exit
alias xx="exit"
 
# joshuto file manager
alias jo='joshuto'

# tree symbols
alias tsym="echo ├ ─ │ └"

# deno
alias dd="deno run --check"

# obsidian
alias obs="cp -r ~/templates/.obsidian .; cp -r ~/templates/.obsidian.vimrc ."


# bash
alias o="bash open ."

# rust and cargo
alias cr="cargo run"
alias cn="cargo new"

# sioyek pdf
alias opdf="sioyek & disown"

# docker
alias docker="doas docker"
# make
alias m="make -s"
# python
alias rp="python3 main.py"

alias tok="cat ~/data/token"

# git

# alias ggr="git-graph"
# alias gcl='gh repo clone'
# alias gpu="git push"
# alias gpl="git pull"
# alias gf="git fetch"
# alias gc="git commit"
# alias gm="git merge"
# alias ga="git add"
# alias gs="git status -s"

# cd
alias ..="z .."
alias ...="..;.."
alias ....="...;.."

alias rm="trash"
alias rf="rm -rf"

alias dr="dotnet run"
alias dn="dotnet new"

#alias vim=  lvim 

alias ctp="cp /home/ahmed/Templates/c-temp"
alias cptp="cp /home/ahmed/Templates/cpp-temp"

alias grep="grep --color=auto"
alias cat="bat -p"
alias c="tput -x clear"
alias manim="python -m manimlib --uhd -w -c black --video_dir ~/code/manim 	"
alias t="tldr"
alias pl="pdflatex"
alias ip="ip -c"
 
##### ABBREVIATIONS #####


# pacman
# abbr p="doas pacman"
# abbr i="doas pacman -S"
# abbr r="doas pacman -Rsn"
# abbr q="doas pacman -Ss"
# abbr s="doas pacman -Sy"
# abbr u="doas pacman -Syu"

# systemd
# abbr sl="doas systemctl"


# wl-clipboard
# abbr -g xl="tee /dev/tty | wl-copy -n"
# abbr -g al="wl-paste"


# git

# abbr -g grc='gh repo clone'
# abbr -g gu="git push"
# abbr -g gp="git pull"
# abbr -g gf="git fetch"
# abbr -g gc="git commit"
# abbr -g gm="git merge"
# abbr -g ga="git add"
# abbr -g gs="git status"
# abbr -g gb="git branch"
# abbr -g gk="git checkout"
# abbr -g gr="git restore"
# abbr -g gg="git-graph"
